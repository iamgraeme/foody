import { StyleSheet } from 'react-native';
import { sizes, colors, fonts } from 'app/config/styles';
export default StyleSheet.create({
  text: {
    fontSize: fonts.title,
    fontWeight: fonts.bold,
    marginBottom: 5
  }
});
